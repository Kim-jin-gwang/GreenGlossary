# NOTE: tensorflow-text는 Windows 휠이 제공되지 않으므로 모델 추론은
# Linux(도커/HF Spaces) 환경 기준입니다. 모델은 TF2 SavedModel(Keras 2) 형식이라
# 학습 당시와 호환되는 2.15 버전으로 고정합니다. (Python 3.11 필요)
tensorflow==2.15.*
tensorflow-text==2.15.*
konlpy>=0.6.0
pandas>=1.3.0
openpyxl>=3.0.0
flask>=2.0.0
gradio>=4.0.0
huggingface_hub>=0.26.0  # download_models.py — HF 모델 저장소에서 KoBERT 다운로드
